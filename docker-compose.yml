version: "3.8"

services:
  frontend:
    build:
      dockerfile: Dockerfile
    restart: always
    networks:
      - host

  nginx:
    build: ./nginx
    ports:
      - 3030:80
    restart: always
    depends_on:
      - frontend
    networks:
      - host

networks:
  host:
    driver: bridge
